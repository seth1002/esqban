// null.cpp : Defines the entry point for the console application.
//

//#include "stdafx.h"


int main(int argc, char* argv[])
{
/*
	int readBytes = 1;
	char buff[BUFF_LEN];

	if( -1 == setmode ( fileno ( stdin ), O_BINARY ) )
	{
		perror( "Cannot set mode" );
		return -1;
	}

	while(readBytes > 0) {
		readBytes = fread( buff+pos, 1, BUFF_LEN-pos, stdin );
	}
*/
	return 0;
}
